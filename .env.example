# Banco (LibSQL / SQLite)
# Local:
DATABASE_URL="file:local.db"
# Produção (Turso): use a URL libsql://... e o token abaixo
# DATABASE_AUTH_TOKEN="seu_token_turso"

# Opcional (legado AI Studio — não usado pelo código atual do Recanto)
# GEMINI_API_KEY=""
# APP_URL="https://seu-dominio.vercel.app"
